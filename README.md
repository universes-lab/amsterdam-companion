# Amsterdam Companion

Telegram bot for learning Dutch.

## Setup

1. Copy `.env.example` to `.env` and fill in the values.
2. Install dependencies: `pip install -r requirements.txt`
3. Run startup validation: `python -c "from diagnostics.startup_validator import validate_environment; validate_environment()"`
